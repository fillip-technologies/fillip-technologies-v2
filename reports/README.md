# Migration reports

This directory stores permanent migration and audit reports for SEO landing-page work.

Each service migration should include a report that records the source sheet, matched pages, created JSON files, redirects, project-only pages, validation results, and build status.

