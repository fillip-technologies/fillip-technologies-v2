"use client";
import { motion } from "framer-motion";import { ArrowRight } from "lucide-react";import type { AIAutomationContent } from "@/lib/service-content/types";import { reveal } from "./animations";
export default function WhyFillipSection({data}:{data:AIAutomationContent["why"]}){return <section className="py-24 lg:py-32"><div className="mx-auto grid max-w-7xl gap-14 px-6 lg:grid-cols-12"><motion.div {...reveal} className="lg:col-span-5"><p className="text-xs font-semibold uppercase tracking-[0.22em] text-primary">{data.badge}</p><h2 className="mt-4 font-display text-5xl leading-none tracking-tight lg:text-6xl">{data.title}</h2><p className="mt-6 max-w-md text-lg leading-relaxed text-ink-muted">{data.description}</p><a href="#contact" className="mt-8 inline-flex items-center gap-2 text-sm font-semibold text-primary">{data.cta}<ArrowRight className="size-4"/></a></motion.div><motion.div {...reveal} className="relative border-l border-border pl-7 sm:pl-10 lg:col-span-7">{data.items.map((item,i)=><div key={item.title} className="relative border-b border-border py-7 first:pt-0 last:border-0"><div className="flex gap-5"><span className="font-mono text-xs text-primary">0{i+1}</span><div><h3 className="font-display text-2xl">{item.title}</h3><p className="mt-2 text-sm leading-relaxed text-ink-muted">{item.description}</p></div></div></div>)}</motion.div></div></section>}
