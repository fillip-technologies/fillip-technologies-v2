"use client";
import { motion } from "framer-motion";import { ArrowUpRight, Bot, CalendarCheck, Database, Headphones, MessageCircleMore, PlugZap, Sparkles, Target } from "lucide-react";import type { AIAutomationContent } from "@/lib/service-content/types";import { reveal } from "./animations";
const icons=[Target,Bot,CalendarCheck,Sparkles,Headphones,MessageCircleMore,Database,PlugZap];
export default function CapabilitiesSection({data}:{data:AIAutomationContent["capabilities"]}){return <section className="bg-surface-dark py-24 text-white lg:py-32"><div className="mx-auto grid max-w-7xl gap-14 px-6 lg:grid-cols-12"><motion.div {...reveal} className="lg:col-span-5"><p className="text-xs font-semibold uppercase tracking-[0.22em] text-primary-glow">{data.badge}</p><h2 className="mt-4 font-display text-5xl leading-none tracking-tight lg:text-6xl">{data.title}<br/><em className="italic text-primary-glow">{data.highlightedTitle}</em></h2><p className="mt-6 max-w-md text-lg leading-relaxed text-white/60">{data.description}</p><div className="mt-10 rounded-2xl border border-white/10 bg-white/[0.04] p-5"><p className="text-sm font-medium">{data.integrationTitle}</p><p className="text-xs text-white/45">{data.integrationDescription}</p></div></motion.div><motion.div {...reveal} className="grid sm:grid-cols-2 lg:col-span-7">{data.items.map((label,i)=>{const Icon=icons[i%icons.length];return <div key={label} className="flex items-center gap-4 border-b border-white/10 py-5 sm:odd:border-r sm:odd:pr-6 sm:even:pl-6"><span className="grid size-10 place-items-center rounded-xl border border-white/10 bg-white/[0.04]"><Icon className="size-4 text-primary-glow" /></span><span className="text-sm font-medium text-white/85">{label}</span><ArrowUpRight className="ml-auto size-4 text-white/20" /></div>})}</motion.div></div></section>}
