"use client";
import { useState } from "react";import { motion } from "framer-motion";import { Bot, CheckCircle2, ChevronRight, Sparkles, Target } from "lucide-react";import type { AIAutomationContent } from "@/lib/service-content/types";import { reveal } from "./animations";
const icons=[Target,Sparkles,Bot];
export default function ShowcaseSection({data}:{data:AIAutomationContent["showcase"]}){const[active,setActive]=useState(0);const scenario=data.scenarios[active]??data.scenarios[0];return <section id="showcase" className="py-24 lg:py-32"><div className="mx-auto max-w-7xl px-6"><motion.div {...reveal} className="max-w-3xl"><p className="text-xs font-semibold uppercase tracking-[0.22em] text-primary">{data.badge}</p><h2 className="mt-4 font-display text-5xl leading-none tracking-tight lg:text-6xl">{data.title} <em className="text-gradient not-italic">{data.highlightedTitle}</em></h2></motion.div><div className="mt-14 grid gap-6 lg:grid-cols-12"><div className="space-y-2 lg:col-span-4">{data.scenarios.map((item,index)=>{const Icon=icons[index%icons.length];return <button key={item.name} onClick={()=>setActive(index)} className={`flex w-full items-center gap-4 rounded-2xl border p-4 text-left ${active===index?'border-primary/25 bg-primary text-white':'border-border bg-surface-elevated'}`}><Icon className="size-4"/><span className="text-sm font-medium">{item.name}</span><ChevronRight className="ml-auto size-4"/></button>})}</div><motion.div key={active} initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} className="overflow-hidden rounded-[1.75rem] border border-border bg-surface shadow-soft lg:col-span-8"><div className="border-b border-border bg-white px-5 py-4"><p className="text-xs uppercase text-ink-muted">{scenario.eyebrow}</p><p className="mt-1 text-sm font-semibold">{scenario.name}</p></div><div className="min-h-[420px] space-y-5 p-5 sm:p-8">{scenario.messages.map((message,index)=><div key={index} className={`flex ${message.ai?'justify-start':'justify-end'}`}>{message.ai&&<span className="mr-3 grid size-8 place-items-center rounded-lg bg-primary/10"><Bot className="size-3.5 text-primary"/></span>}<div className={`max-w-[78%] rounded-2xl px-4 py-3 text-sm ${message.ai?'border border-border bg-white':'bg-primary text-white'}`}>{message.text}</div></div>)}<div className="ml-11 inline-flex items-center gap-2 rounded-full bg-emerald-500/10 px-3 py-2 text-xs text-emerald-700"><CheckCircle2 className="size-3.5"/>{scenario.result}</div></div></motion.div></div></div></section>}
